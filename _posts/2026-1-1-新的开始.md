---
layout: post  # 固定值，代表文章布局
title: "LZ77算法核心原理"  # 笔记标题
date: 2026-01-01 10:00:00  # 发布时间
tags: [算法, 数据压缩]  # 笔记标签，方便分类
---
# LZ77算法核心原理

## 一、滑动窗口结构

LZ77的核心是滑动窗口，分为搜索缓冲区和前瞻缓冲区...

### 代码示例

```python
# 简化版LZ77匹配逻辑
def find_longest_match(search_buf, lookahead_buf):
    max_len = 0
    offset = 0
    for i in range(len(search_buf)):
        # 匹配逻辑...
    return offset, max_len
```
